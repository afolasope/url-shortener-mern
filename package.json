{
  "name": "scissors",
  "version": "1.0.0",
  "description": "Url shortener",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prep": "cd backend && yarn && cd node_modules/geoip-lite && yarn run-script updatedb license_key=$GEOIP_LICENSE_KEY  && cd ../../frontend && yarn && yarn build && rm -rf ../backend/templates  && cp -R dist  ../backend/templates/",
    "build": "cd backend && yarn build ",
    "start": "cd backend && yarn start"
  },
  "author": "Folasope",
  "license": "ISC"
}
